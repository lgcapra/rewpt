*** Nat extended with the inf symbol, no operations defined on it
fmod NAT+ is
 pr NAT .
 sorts Inf Nat+ .
 subsorts Inf Nat < Nat+ . 
 op inf : -> Inf [ctor] .
endfm

view Nat+ from TRIV to NAT+ is
  sort Elt to Nat+ .
endv